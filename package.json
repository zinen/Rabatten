{
  "name": "rabatten",
  "version": "1.1.0",
  "description": "Rabat husker, et hurtigt klik og du kan se om du kan få rabat på siden\r Start med at vælge de sider du får rabat via. Herefter ser du en poup i toppen af skærmen med info omkring rabat på de forskellige hjemmeside som du kan få rabat på.",
  "private": true,
  "browser": "background.js",
  "scripts": {
    "npm:reinstall": "rimraf node_modules && npm cache clean && npm install",
    "lint": "standard --fix src/**.js && standard --verbose src/**.js && cleancss --format beautify src/style.css -o src/style.css",
    "uglify:background": "mkdirp build && terser src/background.js -m -c drop_console  -o build/background.js",
    "uglify:common": "mkdirp build && terser src/common.js -m -c drop_console -o build/common.js",
    "uglify:content": "mkdirp build && terser src/content.js -m -c drop_console  -o build/content.js",
    "uglify:options": "mkdirp build && terser src/options.js -m -c drop_console  -o build/options.js",
    "uglify:popup": "mkdirp build && terser src/popup.js -m -c drop_console  -o build/popup.js",
    "uglify:css": "cleancss src/style.css -o build/style.css",
    "uglify:all": "npm run uglify:background && npm run uglify:common && npm run uglify:content && npm run uglify:options && npm run uglify:popup && npm run uglify:css",
    "copy:all": "ncp src/ build/",
    "utils:version": "node utils/manifest-version.js",
    "build": "rimraf build/ && npm run utils:version && npm run copy:all && npm run uglify:all",
    "upload:chrome": "node -r dotenv/config.js ./node_modules/chrome-webstore-upload-cli/index.js upload --source ./build",
    "monitor:test": "nodemon  --inspect ./test/index.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zinen/Rabatten.git"
  },
  "author": "zinen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zinen/Rabatten/issues"
  },
  "homepage": "https://github.com/zinen/Rabatten#readme",
  "dependencies": {
    "clean-css-cli": "^4.3.0",
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.0",
    "standard": "^14.3.1",
    "terser": "^4.5.0"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^1.2.0",
    "dotenv": "^8.2.0"
  },
  "optionalDependencies": {
    "nodemon": "^2.0.2",
    "puppeteer": "^2.0.0",
    "rewire": "^4.0.1"
  },
  "standard": {
    "globals": [
      "DiscountServices",
      "chrome"
    ],
    "ignore": [
      "node_modules/**",
      "build/*"
    ]
  },
  "nodemonConfig": {
    "ignore": [
      "build/*"
    ]
  }
}
